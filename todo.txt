1) Закончить записи в MongoDB при Update, Delete
2) Реализовать ручку чтения истории определенной задачи из mongo_db ('/{task_id}/history')
task = await repo.get_task_by_id(owner=current_user.id, task_id)


